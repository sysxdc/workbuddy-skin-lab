# 旧组件功能已停用

WorkBuddy 5.3.8 更新了首页结构，依赖固定锚点的自定义文字、场景图标、卡片、宠物和悬浮装饰无法获得跨版本稳定性。

从 `v1.3.1` 起：

- 新主题只允许背景、配色和构图参数。
- 运行时不创建 `[data-wb-module]` 节点。
- `homeHeader`、`copySets` 和旧 `modules` 字段只为读取旧主题保留，注入时全部忽略。
- `run-derived`、模块图片替换、模块文字编辑和 Home 组件验收已禁用。

这是明确的兼容边界，不应通过增加新选择器或重新定位旧模块绕过。
